# Technical Assumptions Register

Numbered `TA-NN` entries recording an assumption made in the absence of a validated fact —
usually because the real-world source (a Summit screen, an actual import result, a live
config) has not been checked against what the code assumes. Each entry states the assumption
and its reversal cost, so a wrong guess can be found and priced later without re-deriving it.

**Entries are appended, never edited in place.** A superseded or resolved assumption gets a
status line noting what changed and when — the original entry stays as written, for
provenance. Numbering is sequential and never reused.

This register did not exist before 2026-09-13 (S57-P2 Phase A finding); TA-9 through TA-13
below are copied verbatim from `docs/session_closeout_2026-09-13_two_file_export.md`, where
they were first recorded.

## Entries

- **TA-9 — `enrollment_matrix_entry.amount` is not emitted on the HRA file.** The 8-column contract has no amount column. **Reversal: low.**
- **TA-10 — column F / I's populated path has never been exercised.** `payroll_frequency`'s `summit_schedule_name` has been NULL on every row to date, so every export has taken the empty-string fallback. **Reversal: low to test.**
- **TA-11 — the column contracts are `[DOC]`,** read off the Summit picker, never validated by an actual import. **Reversal: low now, high later.**
- **TA-12 — `MONTHLY_PREMIUM` → column G annualized (× 12), column H empty, schedule in column I. `ANNUAL_ELECTION` → column G verbatim.** `BigDecimal`, scale 2, HALF_UP. The 125 template has Annual Election Amount and Per Contribution Amount and a monthly premium is neither; this is Claude's call, not Kevin's. **Reversal: two lines** — and it produces wrong dollars in Summit if wrong, so it is the first thing to check against a real import.
- **TA-13 — the schedule name lands in different columns per template:** F (Employer Contribution Schedule) on HRA, I (Participant Contribution Schedule) on 125, on the reasoning that HRA is employer-funded and 125 is employee salary reduction. **Reversal: low.**
- **TA-14 — SSA's standard HRA tier set is DataPath's "Tier Structure 3":** `EE/Only`, `EE/SP`, `EE/CN`, `EE/FAM`, seeded in V109. Adopted because Summit stamps these into the benefit plan's Coverage Levels/Tiers grid from the Tier Group dropdown, so neither side types the string. All HRA plans carry all four tiers even when the contribution does not vary by tier — a flat ICHRA is configured with the same amount on each row. The Tier ID strings are read off the Summit UI, not off an import results file, so the match is unvalidated. `summit_tier_id` carries no unique constraint; two active rows sharing a value would collapse to one option in the picker. **Reversal cost: low** — four seeded rows and one `<select>`'s option list.
- **TA-15 — The matrix schedule dropdown will be filtered from two application answers**, `paycycle_frequency` (free-text dropdown) and `paycycle_first_paydate` (date), widened by `paycycle_other_have`. Day-of-week is treated as the strong signal: a Thursday employer hides all Friday schedules and vice versa. Bi-weekly hides all weekly; weekly keeps same-day bi-weekly. Bi-weekly parity is computed as `daysBetween(first_paydate, anchor_date) mod 14` — `0` is cycle A, `7` is cycle B. Semi-monthly resolves entirely from the answer text, with no date arithmetic. `paycycle_other_have = Yes` drops the cadence and parity rules and keeps only the day rule. An unrecognised or missing answer results in **no filtering**, never partial filtering. **Not yet implemented and not yet validated against real application data.** **Reversal cost: low** — the filter is one query predicate; the metadata columns are additive.
- **TA-16 — `payroll_frequency.application_value` (V107) is retired in favour of `paycycle_frequency_alias` (V110).** The column mapped a `paycycle_frequency` answer to a schedule at per-schedule grain and was read by one preselection path that returned the first match by sort order — wrong for three of four bi-weekly employers, since one answer legitimately matches Thursday/Friday × cycle A/B. The alias table maps answer text to a recurrence token at per-answer grain, and V110's `pay_dow` / `anchor_date` / `semimonthly_variant` columns carry the discrimination the column could not express. Payroll preselection is intentionally absent until the TA-15 filter ships. **Reversal cost: low** — one guarded `ADD COLUMN` and roughly 25 lines restored across five files; no data to restore.
- **TA-17 — The TA-15 suggestion is surfaced, never preselected.** Participant panels are hidden `<div>`s in a single form and hidden inputs submit, so a `selected` suggestion would persist on the next Save for every unlocked participant the operator never opened. The suggested `<optgroup>` renders first and a caption names the single preferred survivor; the operator still clicks. A date that fails strict ISO parsing, an unmatched alias, or a blank frequency answer all yield an empty suggested map and a flat list — never a partial filter. Bi-weekly parity uses `Math.floorMod`, since a first paydate earlier than the anchor is the normal case. **Reversal cost: low** — adding `selected` in the JSP is one word; removing the feature is deleting two optgroups and one method.
- **TA-18 — The enrollment matrix spreadsheet export (`/EnrollmentMatrixExport`, S58-P1) is generated from the rendered grid, not from a fixed column list:** one `.xlsx` row per (participant, leg) pair in page order, with four leading keys — `entry_id` (empty until a row is saved), `participant_id`, `leg` (= `summit_plan_template_map.id`, the tab key), `participant_tpa_custom_id` — then the page's read-only columns (`Participant`, `Leg`, `Leg mode`, `Locked`) and its editable ones (`Payroll frequency`, `Custom schedule name`, `Monthly premium`, `Annual election`, `Tier`, `Declined`), where each `<select>` cell carries the value the page *posts* (schedule code, `summit_tier_id`), not the label, and header-level fields repeat on every leg row. `.xlsx` because POI 5.2.3 was already in `pom.xml`; `Leg mode` is the stored `enrollment_amount_mode`, emitted so a blank row says which of the three amount/tier cells applies. The round-trip key is provisionally `entry_id`, falling back to (`participant_id`, `leg`), and **blank-cell semantics on import are undecided** — import is out of scope. **Reversal cost: low** — one servlet and one link.
- **TA-19 — "Open" and "locked" for GUID-addressed matrix access (S58-P3) are defined as: open = `Activity.is_complete` false on the `Setup` (it carries no status column of its own), locked = `enrollment_matrix.is_pushed` true.** Both are checked on every `/matrix/{guid}` request, not only when the link is issued. Nothing in the codebase sets `is_pushed` today, so the locked check is inert until freeze-on-push lands and arms it. **Reversal cost: low** — two predicates in `EnrollmentMatrixServlet.doGetByGuid`. *S58-P4 note:* definitions and checks unchanged; what changed is where the GUID path lands — its own standalone read-only page (`matrixAgentView25.jsp`) rather than the PSP page with inputs disabled, so no `matrixReadOnly` flag and no `<base>` tag exist any more.
- **TA-20 — Matrix visibility now depends on `OriginatingAgencyResolver`'s `get(0)` tie-break.** `agencyOf(Person)` takes the first entry of `listOfAgenciesWithThisAgent`, an unordered ManyToMany, so for a person in several agencies the resolved originating agency is not guaranteed stable across loads; `MatrixAccessResolver` adopts that answer as-is rather than deriving a second one. A different `get(0)` on a later load could move the matrix into, or out of, a requester's membership set. **Reversal cost: low** — the resolver is one method; an ordered membership query is the fix if it bites. *S58-P4 note:* still true for the agency-side rules, which are unchanged. It now also feeds `resolveSetupPspId`'s *fallback* only — the primary PSP answer is `proposal.rate.psp`, which does not depend on the tie-break (TA-22).
- **TA-21 — The parent-agency reach in `MatrixAccessResolver` is exactly one hop up `parent_agency_id`.** Two levels are enforced by `AgencyAction` (R1–R3), not by the schema, so a hand-created deeper chain leaves a grandparent agency unable to see a matrix it arguably should. No walker is added; the resolver reads `originatingAgency.getParentAgency()` once. **Reversal cost: low** — a loop in place of the single read. *S58-P4 note:* unchanged by the revision; the agency-side rules were not touched.
- **TA-22 — PSP staff access to a matrix is scoped to "the setup's own PSP", and the setup's PSP is read off the sale, not the setup (S58-P4).** `Setup`/`Activity`/`Assignee` carry no PSP column, so `MatrixAccessResolver.resolveSetupPspId` takes `proposal.rate.psp` first (`proposal.rate_id` is `NOT NULL` and every `Rate` is created under a PSP in `RateTableAction`), then the originating agency's `psp_id` as fallback; a setup whose PSP cannot be established admits no staffer at all. The same scope guards `action=issueLink`. *S58-P5 correction:* `isPspSales` **is** admitted, under the same PSP scoping as admin and user — the three flags `AgencyScopeResolver` treats as `pspWide` — in both `MatrixAccessResolver` and `issueLink`; the earlier exclusion was a literal reading of the S58-P4 wording, not a design decision. (`detailSetup25.jsp`'s buttons still show for admin/user only — it was outside S58-P5's fence.) **Reversal cost: low** — one static method and one comparison; unscoping is deleting the comparison.
- **TA-23 — AMS had no post-login return mechanism of any kind before S58-P5; this is the first, and it is deliberately scoped to one URL shape for one feature rather than built as a general facility.** `/matrix/` is exempt from `LoginFilter`; `EnrollmentMatrixServlet.doGetByGuid` applies `LoginFilter`'s own authentication test *before* any GUID lookup (so a logged-out visitor cannot learn which GUIDs exist), captures `servletPath + pathInfo` into one session attribute only when it matches the anchored, fixed-length `^/matrix/<uuid>$` pattern, and `AuthenticateUser.goToPage` consumes it single-use, re-validating against the same pattern and otherwise falling through to the unchanged role routing. The capture is pattern-matched, not sanitised, so no open-redirect surface exists today; widening the pattern (or turning this into "return to any internal path") reintroduces one. Session survival across login was established from the code, not assumed (no `invalidate()`/`changeSessionId()` on the login path). **Reversal cost: low** — one filter entry, one capture block, one consumption point.
- **TA-24 — The matrix completeness rule now lives in two places: `MatrixCompletenessService.isModeValuePopulated` (S58-P7) and `SummitExportServlet.isModeValuePopulated` / its gate loop.** They are byte-for-byte the same rule today (declined = satisfied; `TIER` → tier name non-blank; `ANNUAL_ELECTION`/`MONTHLY_PREMIUM` → amount non-null; any other mode → never satisfied) and must stay so, or the agent page will call complete a matrix the exporter refuses. The service is deliberately *stricter* in one direction only: a participant with no header row, or a leg with no entry row, is invisible to the exporter but reported as work to do here — so "complete here ⇒ accepted there" holds, not the converse (an unsaved participant exports as silently absent). The exporter should later delegate to the service; that file was fenced this run. **Reversal cost: low now, rising while both exist.** *S58-P10 note:* a third divergence since S58-P9 — a participant on a non-enrollment-approved payroll schedule is not `COMPLETE` in the service, but the exporter still emits them with whatever `summit_schedule_name` the row carries; direction unchanged, complete-here still implies accepted-there.
- **TA-25 — Agents now write data that exports to Summit, and `enrollment_matrix_entry` records no provenance for a changed value.** Its columns are `amount`, `tier_name`, `is_declined`, `declined_at`, `recorded_by` (set only on the declined false→true flip), `created_at`, `created_by` — no `updated_at`/`updated_by`. After S58-P7 an amount typed by an agent and one typed by PSP are indistinguishable, and a later correction leaves no trace of who changed what or when. **Reversal cost: low to add (two columns and one line in the shared entry-write loop); the missing history is not recoverable.**
- **TA-26 — "Needs PSP confirmation" is derived from `enrollment_matrix_participant.payroll_frequency` being `OTHER_CUSTOM` or `OTHER_NOT_IMPORTABLE`, not stored.** The agent's explanation goes in `agent_schedule_note` (V113); PSP resolving the state means selecting a real schedule on the PSP page, which clears it with no second write. A participant in that state is not `COMPLETE` however full their cells are. Consequence worth knowing: the exporter itself neither refuses nor warns on either sentinel — `OTHER_NOT_IMPORTABLE` silently drops the participant and `OTHER_CUSTOM` with a blank custom name silently emits an empty schedule column — so this derived state on the agent and PSP pages is the only place the condition is surfaced. **Reversal cost: low.**

The following (TA-27–TA-44) were all proposed, unfiled, across the S59 Summit-setup-panel restructuring
(runs P2 through P10) and are filed together here at close-out (S59-P11), each tagged with the run that
produced it. TA-36 supersedes S59-P6rev's original four-state count with S59-P7's corrected eight-state
one, filed once rather than twice. TA-44 reconciles S59-P5rev's own proposed text, which described a
hierarchy-by-indentation that S59-P8 subsequently removed entirely.

- **TA-27 — `SummitEmployerLinkServlet`'s `mode` parameter contract (absent / `url` / `status`), one availability-chain evaluation for all three.** `mode` absent is byte-identical to the servlet's pre-S59-P2 behaviour; `mode=url` writes the resolved Summit URL alone (escaped, no markup) and nothing on any failure; `mode=status` is byte-identical to absent except the non-tab success case drops the anchor, keeping the identity text. The seven-condition availability chain (session `isPspAdmin`, `IchraAccessResolver.isAvailable`, proposal→prospect, key resolution, `SummitEmployerLookupDAO` match, single distinct `altId`, `buildEditEmployerUrl` non-null) is evaluated once, identically, regardless of mode — never reordered, short-circuited or relaxed per mode. Risk if wrong: a future edit to one branch's failure text without mirroring the guard in every mode could let `mode=url` leak a status string, or vice versa. **Reversal cost: low** — each `if (urlMode)`/`if (statusMode)` line is independently removable; the `doGet` mode read is a two-line revert. (S59-P2)
- **TA-28 — Two Summit lookups per Employer/Plans row render, deliberately uncached.** S59-P2's title-link fragments (`mode=url` for the link, `mode=status` for the identity line) each independently re-run the full availability chain rather than sharing one resolution within the request — an explicit instruction not to add a request-scope memo or any other optimisation for that build. Acceptable on a PSP-admin-only page with low concurrent traffic. Risk if wrong: if this page's traffic or the lookup's cost grows, each row render costs 2 EntityManager-backed HTTP round trips instead of 1 (Employer: 2, Plans: 2 — was 2 total for the whole panel before S59-P2, now 4 for these two rows combined). **Reversal cost: low** — a request-scope cache keyed on `(proposalId, tab)` could be added inside the servlet later without changing its external contract. (S59-P2)
- **TA-29 — The card-issuer row's visibility guard is three-valued and fails open on anything indeterminate.** `CardIssuerAvailabilityService.Result` is `AVAILABLE`/`NOT_AVAILABLE`/`INDETERMINATE`; only `NOT_AVAILABLE` (active `summit_plan_template_map` rows exist for the PSP and none, restricted to elected service items, is flagged `is_card_issuer`) hides the row. No active mapping rows (property-fallback territory, every fresh installation), an unresolvable PSP, or any exception all resolve to `INDETERMINATE`, rendered exactly as `AVAILABLE`. Basis: hiding a working control on a lookup failure, or because a PSP hasn't yet adopted table-based mapping, is worse than showing a row that turns out not to apply. Risk if wrong: a PSP mid-migration to table-based mapping (some rows entered, card-issuer not yet flagged) is indistinguishable from "genuinely not applicable" and sees the row hidden either way. **Reversal cost: low** — the guard is two `<c:if>` blocks around otherwise-unmodified markup. (S59-P3rev)
- **TA-30 — ⚠️ The `keySegmentRejection` gap, carried deliberately: a PSP whose active mapping rows all fail `SummitPlanTemplateResolver`'s private key-segment validation looks row-backed to `CardIssuerAvailabilityService`, which reads `SummitPlanTemplateMapDAO` directly with no key-segment check of its own, while the resolver itself would fall through to the property fallback for the actual export.** In that narrow case the service could return `NOT_AVAILABLE` (hiding the row) in a state the export path would treat as property-fallback indeterminate. Closing this would mean either duplicating the resolver's private validation in the service, or exposing the resolver's deliberately-opaque table-vs-property source distinction (its own javadoc: "Callers cannot tell which source answered, and must not learn") — both rejected as the wrong fix. Risk if wrong: requires every active row for one PSP to independently fail key-segment validation — a data-quality state the admin screen's own property-entry-count display already tolerates via the same non-strict skip-with-WARN path. **Reversal cost: low, but not free** — closing it needs either a duplicated validation copy that could drift, or a resolver design change out of that item's original fence. (S59-P3rev)
- **TA-31 — Two callers determine the card-issuer condition independently: `SummitExportServlet.writeCardSeedElection` (table-then-property, validated, refuses on ambiguity) and `CardIssuerAvailabilityService` (table-only, unvalidated, three-valued, never refuses).** They answer different questions — export: "which one plan do I actually emit"; guard: "should a row exist at all" — and unifying them would require exposing the resolver's deliberately-hidden source distinction (TA-30) or duplicating its private validation. Risk if wrong: any future change to `is_card_issuer` semantics, the elected-service-items query shape, or the mapping table's schema must be applied by hand in both places; a change to only one silently desynchronizes the guard from the export's actual behavior. **Reversal cost: medium** — unifying them later (the export delegating to the service, or vice versa) is a real refactor, not a two-line revert, since the two methods currently have different failure semantics (the export throws/refuses; the service always returns a value). (S59-P3rev)
- **TA-32 — ⚠️ Bootstrap's JS bundle is load-bearing for this panel's information, not just its polish.** Every section's description and status text lives behind a `.collapse`-gated help toggle; `.collapse` is inert without `bootstrap.bundle.min.js`. Confirmed loaded unconditionally: `activityDetail25.jsp:7` imports `css-js.jsp` unconditionally in `<head>`, which emits the bundle `<script>` tag (not merely the CSS) unconditionally. No description or status string is duplicated anywhere else on the page as a fallback. Risk if wrong: if `css-js.jsp` is ever changed to load the bundle conditionally, load it after this panel's markup, or swap it for a CSS-only Bootstrap build, every description line and status string on this panel becomes permanently unreachable — a silent, total information loss on a page that serves every line of service, discoverable only by someone clicking a toggle and finding it does nothing. **Reversal cost: low** for the toggles themselves (unwrapping each `.collapse` restores always-visible behavior); the dependency risk itself is only mitigated by never weakening `css-js.jsp`'s bundle load without re-auditing every `.collapse`/`data-bs-toggle` consumer repo-wide. (S59-P4)
- **TA-33 — The merged Census row's seven-state lifecycle and its precedence order (`TERMINAL` > `ROSTER_LOADED` > `AWAITING_REVIEW` > `REVOKED` > `LINK_EXPIRED` > `AWAITING_CLIENT` > `NOT_REQUESTED`) exhaustively and correctly classify every reachable combination of `census_request`/`census_submission`/`employer_participant`/`summit_setup_step`/`summit_file_export` state for a Setup.** `CensusLifecycleService.resolve` evaluates a single ordered if-chain, first match wins, built entirely on existing accessors (`CensusIntakeService.statusFor`, `EmployerParticipantDAO.countByProspectId`, `CensusIntakeService.demographicsSettled`) rather than new query logic. Risk if wrong: a state combination not anticipated by the table (e.g. a `REVOKED` request with a stray `PENDING` submission — resolved here by `AWAITING_REVIEW` winning per the stated precedence) resolves silently to whichever branch it hits first, with no signal it's an edge case. **Reversal cost: low** — the precedence order is one ordered chain in one method; reordering or adding a branch touches neither the servlet nor the JSP's action-set shape, only which token comes back. (S59-P6rev)
- **TA-34 — Roster presence stands in for a missing `census_request` row on PSP-side uploads.** `CensusUploadServlet` writes `employer_participant` directly with no `census_request`/`census_submission` row, so `ROSTER_LOADED` is reached by *either* `request LOADED` *or* `EmployerParticipantDAO.countByProspectId > 0`, checked before the submission-based `AWAITING_REVIEW` branch — a roster loaded by hand always outranks a stale pending submission. Basis: without this check a roster loaded by hand would sit at `NOT_REQUESTED` forever, which is exactly the case most likely to be hit while testing. Risk if wrong: if a future load path writes `employer_participant` rows provisionally (a staging step that doesn't mean "loaded"), this state machine would report `ROSTER_LOADED` prematurely, hiding the Request/Upload controls a PSP admin might still need. **Reversal cost: low** — the roster check is one `OR` condition in one branch. (S59-P6rev)
- **TA-35 — `INDETERMINATE` (and any unrecognized state token) fails open to the full legacy action set in the merged Census row.** Basis: this session's repeated design rule — showing every possible control when the state can't be determined is strictly safer than showing none or guessing a subset; every underlying action still enforces its own real preconditions server-side. Risk if wrong: a PSP admin sees controls that don't actually apply to the setup's true state if the lookup is failing silently and repeatedly rather than as a rare transient blip — a UX confusion, not a data-safety issue. **Reversal cost: low** — the fail-open branch is self-contained; narrowing it doesn't touch any other branch. (S59-P6rev)
- **TA-36 — Revoke remains reachable only via the compose page (`censusRequest25.jsp`), not as a one-click control in the merged Census row; the envelope's `title` varies by state across all eight states, not four.** *(Supersedes S59-P6rev's original four-state count — S59-P7 extended the varying-title treatment to `AWAITING_REVIEW`, `ROSTER_LOADED` and `TERMINAL`, all "Request an updated census", and made `INDETERMINATE` an explicit fifth branch, so every one of the eight `CensusLifecycleService.State` values now produces a distinct or shared title, not just four.)* Basis: a PSP admin revoking an active client link needs to see what was sent and when it expires before doing it — a decision, not a reflex — so the envelope link (its `href`/`class`/icon identical in every state, only `title` narrating what's available) is sufficient; a bare one-click Revoke button on the summary row would remove exactly the context that makes revoking safe. `Revoke`/`Renew` were found in S59-P6 (which correctly hard-stopped) to have no existing relocatable row-level control at all — they exist only as `action=revoke`/`action=send` POST handlers on the compose page. Risk if wrong: this design costs one extra navigation step (click envelope → land on compose page → click Revoke) versus a hypothetical one-click row-level Revoke. **Reversal cost: medium** — adding a true one-click Revoke means either a new POST-capable control on this row (touching the compose page's server-side handling or duplicating it) or a confirm-and-post pattern matching this panel's existing hidden-form idiom. (S59-P6rev / S59-P7)
- **TA-37 — The Census row renders one fixed control sequence with per-control visibility; no per-state control lists.** S59-P6rev's original design gated whole action-set lists per state, one list per `<c:when>` branch; eight independent lists let order and presence drift — three states silently dropped the envelope, and `ROSTER_LOADED` appended Re-upload last while every other state put it second, a real hazard next to Push. S59-P7 replaced this with one fixed sequence (envelope, upload, preview, download, push, check response, mark done), each control appearing exactly once and carrying its own `or`-condition visibility instead of being copied into whichever states need it — order can no longer diverge because there is only one order. Risk if wrong: a control added later must be inserted at the correct fixed position, not appended to a state-specific list; inserting it in the wrong place puts it in the wrong visual position in every state it applies to, silently. There is no compiler or template check that catches a misplaced insertion. **Reversal cost: low** — the whole block is self-contained (one title-selecting `<c:choose>`, one `.btn-group` with unconditional lines plus per-control `<c:if>`s). (S59-P7)
- **TA-38 — Requesting a census (the envelope control) is legitimate in every one of the eight states, with no state where it should be hidden.** S59-P6rev's original design scoped the envelope to four states, as if requesting an updated census stopped being legitimate once a roster or file existed — a defect, corrected in S59-P7 by making the envelope unconditional, varying only its `title`. Risk if wrong: if a future state genuinely should not offer re-requesting (e.g. some terminal-and-locked state not yet modeled), the envelope would still render there since it is no longer individually gated — a state would need an explicit `<c:if>` carve-out added. **Reversal cost: low** — wrapping the envelope's one line in its own `<c:if>` (matching the pattern the other five controls already use) is a two-line change. (S59-P7)
- **TA-39 — The Enrollment line's Download/Check-response/Push choosers offer both file types (`125 PI Elections`, `HRA Enrollment`) unconditionally and rely entirely on `SummitExportServlet`'s existing refusals rather than pre-filtering.** Basis: which file actually applies to a setup is computed inside `SummitExportServlet` (per-leg `import_file_type` routing, the unassigned-route refusal, the TIER-on-125 refusal); building a shared applicability service would either duplicate that logic or require modifying the servlet, both out of scope this run. Risk if wrong: a PSP admin on a setup with only one applicable file type sees a chooser offering a second option that will always refuse — extra friction, not a correctness bug, since the refusal message already names what to fix. **Reversal cost: low** — once `SummitExportServlet` is refactored to delegate to a real shared applicability service (T253), each chooser's two `<li>` items can gain a `<c:if>` wrapping an applicability check without touching anything else in the block. (S59-P8)
- **TA-40 — Push's separation from the read-only Download/Check-response choosers on the Enrollment line is spacing and a Bootstrap `.vr` divider, not a distinct confirm surface.** Basis: visual separation plus the existing per-item `onclick="return confirm(...)"` dialog is judged sufficient protection against a mis-click reaching the destructive action; nothing asked for a second confirmation layer beyond what already existed. Risk if wrong: a `.vr` divider is a comparatively subtle visual cue; the existing `onclick` confirm dialogs remain the actual functional backstop regardless of surrounding markup. **Reversal cost: low** — the divider is one line; strengthening separation later (a distinct color, an icon-based warning) doesn't require restructuring the chooser. (S59-P8)
- **TA-41 — The three controls relocated into the panel from `detailSetup25.jsp` (Enrollment Matrix, Copy matrix link, Open agent view) are now PSP-admin-only, narrowed from PSP-admin-or-PSP-user.** Their prior independent `isPspAdmin or isPspUser` `<c:if>` guards were removed entirely on arrival in favor of the panel's own `local.isPspAdmin()` outer gate — Kevin's explicit, approved decision. Risk if wrong: a PSP user who relied on quick access to these three controls from the setup detail view now has no path to them from this page at all; their only surviving route, if any, is the server-side `isPspStaff`-gated `EnrollmentMatrixServlet.doPost action=issueLink`, reachable only through a UI control that no longer exists for them. **Reversal cost: medium** — restoring PSP-user access means either widening the whole panel's gate (T252) or re-adding a narrower `isPspUser`-aware condition specifically around these three controls, reopening the dual-admin-check-mechanism question this session has repeatedly declined to resolve. (S59-P8)
- **TA-42 — Every control in the Summit setup panel is now icon-only; every control's accessible name depends entirely on `aria-label` rather than visible text.** S59-P9 converted the last three text-labeled controls (Enrollment Matrix, Copy matrix link, Open agent view) to icon-only, matching every other control in the panel, adding `aria-label` mirroring `title` for all three since the icon alone carries no accessible name once the label text is gone. Risk if wrong: any future control added to this panel without both `title` and `aria-label` (or with them saying different things) silently produces an icon with no accessible name, or a confusing mismatch between hover text and screen-reader text — there is no compile-time or template check catching this. **Reversal cost: low** — restoring visible text to any one control, if ever needed, is a small, local, per-control edit; it doesn't touch any other control's markup. (S59-P9)
- **TA-43 — The display title `Card Issuance` is decoupled from the `cardseed` step name used by the toggle id, the export `type=`, the hidden form id and the status include's `step=`; a future reader must not infer the underlying Summit step name from the visible title.** S59-P10 renamed only the visible row title (from `$1 card-issuer seed election` to `Card Issuance`, matching the other top-level rows' `fw-semibold` title pattern); `summitHelp-cardseed`, `type=cardseed`, `form="summitPush-cardseed"` and `step="cardseed"` all still key off the original Summit step name, none of them `Card Issuance`. Risk if wrong: a future maintainer searching the file for "Card Issuance" to find the export/push/status wiring will find nothing — they must know to search `cardseed` instead; conversely, someone reading `cardseed` in Java code or a URL has no textual link back to what the PSP admin actually sees on screen. **Reversal cost: low** — this is presentation-only; nothing downstream depends on the display string, so it can be renamed again freely without touching the four `cardseed`-keyed references. (S59-P10)
- **TA-44 — The Summit setup panel conveys step order by document position alone, with no numbering and no hierarchy levels at all — the panel is flat.** S59-P5rev's own proposed assumption (order by document position, sub-step relationship by indentation) is stale: the two indented sub-rows it described (`125 PI Elections`, `HRA Enrollment`) were both consumed by S59-P8's Enrollment-line consolidation, and `padding-left: 2rem` occurrences in the file are now zero — there is no remaining indentation anywhere in the panel; every row is a flat top-level peer. Basis: S59-P5rev removed the step-number badges on the reasoning that stacking order alone was sufficient; S59-P8 subsequently removed the one remaining structural signal (indentation) by folding both sub-rows into their parent's own line. Risk if wrong: any future row must be inserted at the exact correct document position to read as correctly sequenced — there is no numbering and no indentation to catch a row accidentally inserted in the wrong place, and no visual cue distinguishes "new top-level row" from anything else. **Reversal cost: low** for restoring badges specifically if ever needed (S59-P5rev's own report describes the six deleted `<span>` elements verbatim); not free, since any future numbering needs its own fresh design rather than reverting to S59-P5rev's superseded computed-counter approach. (S59-P5rev, reconciled S59-P11)

The following (TA-45–TA-50) were filed at S60's close-out, covering the `/SummitLink` employer
deep-link feature built across S60-P1 through P5. TA-46 is the one falsification test in the set —
its predecessor claim (an unqualified consistency observation) is not separately filed, since it was
never asserted as verified.

- **TA-45 — Summit holds the selected employer in server-side session state, shared across browser tabs.** Basis: runtime-verified by Kevin this session — loading `EditEmployer.aspx?employerId=N`, then opening a Summit page carrying no identifier of its own, in a different tab, landed on employer N; verified against an employer whose context was not already set, correcting S60-P2's earlier false-positive verification against an employer whose context already matched (see this session's close-out, "Contradictions found"). Design choice: every `CONTEXT_THEN_PATH` catalog page relies on this holding. Risk if wrong: a two-hop link silently renders a correct-looking page for the wrong employer — not an error, a misdirection, which is why the S60-P4 auto-sequence was removed rather than tuned. **Reversal cost: high** if it ever stops holding — every `CONTEXT_THEN_PATH` page would need its own identifier parameter, if Summit exposes one, or the mode would have to be dropped from the catalog entirely. Status: runtime-verified. (S60)
- **TA-46 — `/Area/Participant/ParticipantList?employerId=` takes Summit's `EmployerID` — the same value `EditEmployer.aspx?employerId=` takes, which AMS holds as `Employer.altId`.** Basis: runtime-verified by a falsification test, not mere consistency — Summit's session context was set to employer A, then `?page=participants` from employer B's Setup landed on employer B, not A, showing the URL parameter itself (not leftover session context) determines the page. Not `Employer.id`/`organization_id`; the parameter name matching and Kevin's example URL being consistent with `altId` were the state of the evidence before this test (S60-P5's stated open question), not confirmation. Risk if wrong: an AMS-internal id in that parameter would have opened an unrelated or nonexistent Summit employer, silently. **Reversal cost: low** — one catalog entry's mode/template string. Status: runtime-verified. (S60)
- **TA-47 — `/Area/Employer/BankAccounts` honours Summit's session employer context with no query parameter of its own.** Basis: runtime-verified this session. ⚠️ An earlier example URL for this page carried `?organizationId=`, deliberately not used here — AMS cannot establish which Summit numbering space that parameter expects (S60-P1's Q4: `OrganizationID` and `EmployerID` are different, numerically overlapping spaces), and the paramless `CONTEXT_THEN_PATH` form sidesteps that question rather than resolving it. Risk if wrong: would have meant this link landing on whichever employer Summit last had selected — the same general failure mode TA-45 describes, closed here for this specific page. **Reversal cost: low** — one catalog entry's mode. Status: runtime-verified for the paramless form. (S60)
- **TA-48 — The Summit page catalog is code, not PSP-scoped reference data.** Basis: Summit page paths are properties of the Summit product itself, identical across every installation; the only installation-specific values (`SUMMIT_PATH`, `SUMMIT_TPA_GUID`) are already constant rows, and they are unrelated to which pages exist. Design choice: in-code `SummitPageCatalog` (an immutable, in-memory map of `record`s), no table, no migration, no reference-data rows, no admin CRUD screen — adding a page is a one-line code change. Risk if wrong: if page sets ever need to vary per PSP or per agency, this design has no seam for that at all; every installation sees the same seven-page catalog. **Reversal cost: moderate** — a `summit_page_link` table, a migration, a DAO and an admin CRUD page; the servlet's two call sites (`SummitPageCatalog.find`, `.all()`) would not need to change shape, only their implementation. Confirm-before trigger: Kevin wanting to add or rename pages himself outside a code change, or per-agency page sets. Status: design decision, reaffirmed unchanged across S60-P3 (seven pages) and S60-P5 (third mode) rather than reopened either time. (S60)
- **TA-49 — `Cards`, `BenefitPlans` and `Schedules` are the only evidenced `EditEmployer.aspx` `tab` values.** Basis: real URLs supplied by Kevin, one per value. `Cards` is runtime-verified through `/SummitLink`; `BenefitPlans` and `Schedules` are in the catalog (`benefit-plans`, `pay-schedules`) but not yet exercised through `/SummitLink` itself. Other tab names visible in Summit's own UI remain unverified and are deliberately not in the catalog — no value was added speculatively. Risk if wrong: an unverified tab name that turns out wrong would 404 or silently land on Summit's default tab, discoverable only by clicking through. **Reversal cost: low** — one catalog entry's `tab` string. Status: `Cards` runtime-verified; `BenefitPlans`/`Schedules` unverified. (S60)
- **TA-50 — AMS holds no per-employer Summit GUID, and this design no longer needs one.** Basis: S60-P1's repo-wide and migration-wide search found no `employer_guid`/`employerGuid`/`organization_guid` anywhere in source, migrations, docs or config; the only Summit GUID in the codebase, `SUMMIT_TPA_GUID`, is tenant-wide, not per-employer. Design choice: every Summit page in the catalog is reached from `Employer.altId` alone, via one of three modes (`DIRECT`, `CONTEXT_THEN_PATH`, `DIRECT_PATH`) — the missing GUID and the ambiguous `organizationId` (S60-P1's Q4) both stopped mattering once Summit's session employer context (TA-45) was found to make `altId` sufficient for every page in scope. Risk if wrong: n/a — this removes a dependency rather than adding one; a future Summit page that genuinely required a GUID or an `organizationId` AMS doesn't hold simply couldn't be added to the catalog, the same limitation that existed before S60. **Reversal cost: n/a.** Status: design decision, closed. (S60)
- **TA-51 — `SummitImportService.importEmployers` (the `/SummitImport` J1 wizard) is the sole writer of `Employer.altId`, and it repairs rows left at `0` or previously written wrong on its next run against a 21-column export.** Basis: `SummitImportService.java:317` — `if (employerId > 0 && existing.getAltId() != employerId) existing.setAltId(employerId)`; read, not runtime-verified. Risk if wrong: legacy-created employers stay at `altId = 0` indefinitely and every Summit deep link for them refuses; no wrong page is opened, so the failure is visible rather than silent. **Reversal cost: low** — code revert of the `Updater.java:80` change restores the prior behavior. Confirm before: relying on a re-import to correct rows 1400/1401 or any other legacy-created employer. Status: read, not runtime-verified. (S61)
- **TA-52 — The J1 employer export does not currently land in Summit's SFTP `ExportFiles` directory; no observation of one exists in the tree.** Basis: `docs/business/summit_data_exchange.md` records two Summit-written files in `ExportFiles` (2023 and 2025, neither a J1) and one audit-driven participant export (`ZZ_AMS_AUDIT_PARTICIPANTS_Export_…`); every J1 that has ever entered AMS came through the `/SummitImport` multipart wizard. `SummitRefreshService` (S61-P6) is therefore **inert until Kevin configures that export Summit-side** with a template named `ZZ_J1_Employer` (so the file is `ZZ_J1_Employer_Export_{yyyyMMddHHmmssSSS}.CSV`) — Summit configuration, out of bounds for AMS. Risk if wrong: none to data — a tick with no matching file is a recorded no-op (`audit_run` status `OK`, summary "No-op — …"), and a tick that finds a file simply runs the same importer the wizard runs. **Reversal cost: none.** Confirm before: treating AMS employer data as current on the strength of the scheduler existing, or of `SUMMIT_REFRESH_ENABLED` being set. Status: unverified by construction. (S61)
- **TA-53 — `audit_run` is an acceptable home for Summit refresh run records without a schema change.** Basis: the columns read from `V099__audit_run.sql` and `AuditRun.java` during S61-P6 — `id` (auto), `psp_id BIGINT NULL`, `check_key VARCHAR(50) NOT NULL`, `run_at DATETIME NOT NULL`, `run_trigger VARCHAR(10) NOT NULL`, `status VARCHAR(16) NOT NULL`, `finding_count INT NOT NULL`, `summary VARCHAR(500) NULL`, `error VARCHAR(500) NULL`, `duration_ms INT NULL` — and the values written: `check_key = SUMMIT_REFRESH_J1` (17 chars), `run_trigger` ∈ {`SCHEDULED`, `MANUAL`} (≤ 9), `status` ∈ {`OK`, `ERROR`, `SKIPPED`, `NOT_CONFIGURED`} (≤ 14; `SKIPPED` is new vocabulary but the column is an unconstrained VARCHAR), `finding_count` always 0, `summary`/`error` truncated to 500. Written through the existing `AuditRunDAO.insert`, read back through `AuditRunDAO.findLatestPerCheck`. Two side-effects accepted: `AuditService`'s constructor loads every latest row for the PSP into its map, so an `ERROR` refresh row lights the audit navbar badge (`getErrorCount()`), and the `import_run_log` table was rejected because `run_by BIGINT NOT NULL` has no person for a scheduled run. Risk if wrong: run history is thin (no per-row counts as columns, only in `summary`) or mistyped (`SKIPPED` is not in `AuditResult`'s vocabulary). **Reversal cost: low** — a migration adding a purpose-built `summit_refresh_run` table, with `SummitRefreshService.record` as the only caller to repoint. (S61)
- **TA-54 — Summit's export filename timestamp is not a fixed 17 digits.** Basis: runtime, 2026-09-14 — the `ZZ_J1_Employer` export Kevin configured landed as `ZZ_J1_Employer_Export_2026091410080821.CSV`, a 16-digit timestamp, where the participant audit export (and every export observed before this one) emits 17; the fractional part's meaning — hundredths of a second, or a millisecond value with a leading zero stripped — is not determinable from one observation. Design choice: `SummitRefreshService`'s own copy of the timestamp pattern now matches 14 to 17 digits; ordering and the age check use only the leading 14 (second precision), never the fractional group, so no padding-direction assumption is built into the code. Risk if wrong: two exports landing inside the same second could be selected in either order — harmless, since the import is idempotent. **Reversal cost: low** — narrowing the pattern back to 17 digits is a one-line revert, though it would reintroduce the exact failure this closes. Confirm before: relying on sub-second ordering of Summit exports anywhere. (S61)
